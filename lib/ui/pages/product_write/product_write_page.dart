import 'package:flutter/material.dart';

class ProductWritePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('ProductWrite'),
    );
  }
}
