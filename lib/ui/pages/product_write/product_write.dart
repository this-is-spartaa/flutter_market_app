import 'package:flutter/material.dart';

class ProductWrite extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('ProductWrite'),
    );
  }
}
